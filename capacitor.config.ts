import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.atlas.mpos',
  appName: 'Atlas Bank MPOS',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
